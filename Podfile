# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'OrderOut' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!
  use_modular_headers!
  
  # Pods for OrderOut
  pod 'Swinject', '2.6.0'
  pod 'SwinjectStoryboard'
  pod 'Bond'

  target 'OrderOutTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
